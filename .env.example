# Google Places API key (required)
# Get from: https://console.cloud.google.com/apis/credentials
# Enable: Places API (New)
GOOGLE_PLACES_API_KEY=your_google_places_api_key_here

# Resy credentials (optional — enables automated booking)
# API key: found in Resy web app network requests (X-Resy-Universal-Auth header)
# Auth token: found after login (X-Resy-Auth-Token header)
RESY_API_KEY=your_resy_api_key_here
RESY_AUTH_TOKEN=your_resy_auth_token_here

# Data directory override (optional)
# Default: ~/.oakley-bookings/data/
# OAKLEY_BOOKINGS_DATA_DIR=/custom/path/to/data

# Google Calendar (handled by gog CLI, not this skill)
# Required env var for gog: GOG_KEYRING_PASSWORD=oakley
